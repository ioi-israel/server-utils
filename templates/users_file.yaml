# Template for a users file.
# Each entry must contain: "username", "password". Optional fields:
# first_name (default is an empty string).
# last_name (default is an empty string).
# hidden (if true, the user is hidden from scoreboards. Default is false).
# unrestricted (if true, the user has unlimited submission access regardless
# of time. Default is false).

- username:     "john.doe"
  password:     "hunter2"
  first_name:   "John"
  last_name:    "Doe"
  hidden:       true
  unrestricted: true

- username:   "jane.doe"
  password:   "huntress2"
